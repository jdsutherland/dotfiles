# Omarchy/Linux-specific mpv settings.

# Force the mature VA-API path instead of letting FFmpeg select Vulkan Video;
# the Intel driver on this machine does not expose Vulkan AV1 decoding.
hwdec=vaapi

# gpu-next/libplacebo cannot map ewa_lanczossoft on this Linux build.
cscale=ewa_lanczos
