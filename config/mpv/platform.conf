# macOS-specific mpv settings. The tag-omarchy version replaces this file on Linux.
macos-fs-animation-duration=0

# Prefer native VideoToolbox decoding while retaining software fallback.
hwdec=auto

# Chroma scaler used by the macOS video output stack.
cscale=ewa_lanczossoft
